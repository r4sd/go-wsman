# ============================================
# このワークフローは無効化されています
# 有効化するには /setup-cicd スキルを使用してください
# ============================================
#
# name: AI Review Engine (Plan JSON Based)
# 
# on:
#   workflow_call:
#     inputs:
#       pr-number:
#         description: 'Pull request number'
#         required: true
#         type: number
# 
# jobs:
#   review:
#     name: AI レビュー
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
# 
#       - name: Download Plan Artifacts
#         uses: actions/download-artifact@v4
#         with:
#           name: terraform-plan
#           path: terraform-plan
# 
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
# 
#       - name: Extract Plan Summary
#         run: |
#           node .github/scripts/extract-plan-summary.js terraform-plan/plan.json > summary.json
#           echo "=== Extracted Summary ==="
#           cat summary.json
# 
#       - name: Generate AI Review
#         id: review
#         run: |
#           node .github/scripts/plan-analyzer.js summary.json > review.md
#           echo "=== Generated Review ==="
#           cat review.md
# 
#       - name: Post Review Comment
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const review = fs.readFileSync('review.md', 'utf8');
# 
#             await github.rest.issues.createComment({
#               issue_number: ${{ inputs.pr-number }},
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: review
#             });
