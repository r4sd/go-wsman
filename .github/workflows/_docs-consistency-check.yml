# ============================================
# このワークフローは無効化されています
# 有効化するには /setup-cicd スキルを使用してください
# ============================================
#
# name: Docs Consistency Check
# 
# on:
#   workflow_call:
#     inputs:
#       check-paths:
#         description: 'Paths to check (comma-separated)'
#         required: false
#         type: string
#         default: 'docs,README.md,*.tf,*.tfvars'
# 
# jobs:
#   check:
#     name: ドキュメント整合性チェック
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
# 
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
# 
#       - name: Run Consistency Check
#         id: check
#         run: |
#           node .github/scripts/check-docs-consistency.js '${{ inputs.check-paths }}' > result.json
#           echo "=== Consistency Check Result ==="
#           cat result.json
# 
#       - name: Generate Report
#         id: report
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const fs = require('fs');
#             const result = JSON.parse(fs.readFileSync('result.json', 'utf8'));
# 
#             let report = '## 📋 ドキュメント整合性チェック\n\n';
# 
#             if (result.issues.length === 0) {
#               report += '✅ 問題は見つかりませんでした。\n';
#               core.setOutput('has_issues', 'false');
#             } else {
#               report += `⚠️ ${result.issues.length}件の不整合が見つかりました。\n\n`;
#               report += '| ファイル | 問題 | 詳細 |\n';
#               report += '|----------|------|------|\n';
# 
#               for (const issue of result.issues) {
#                 report += `| \`${issue.file}\` | ${issue.type} | ${issue.detail} |\n`;
#               }
# 
#               report += '\n### 推奨アクション\n\n';
#               for (const issue of result.issues) {
#                 if (issue.suggestion) {
#                   report += `- ${issue.suggestion}\n`;
#                 }
#               }
# 
#               core.setOutput('has_issues', 'true');
#             }
# 
#             fs.writeFileSync('report.md', report);
#             console.log(report);
#             return report;
# 
#       - name: Post PR Comment
#         if: github.event_name == 'pull_request'
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const report = fs.readFileSync('report.md', 'utf8');
# 
#             // 既存のコメントを探す
#             const { data: comments } = await github.rest.issues.listComments({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo
#             });
# 
#             const botComment = comments.find(c =>
#               c.user.type === 'Bot' &&
#               c.body.includes('ドキュメント整合性チェック')
#             );
# 
#             if (botComment) {
#               // 既存コメントを更新
#               await github.rest.issues.updateComment({
#                 comment_id: botComment.id,
#                 owner: context.repo.owner,
#                 repo: context.repo.repo,
#                 body: report
#               });
#             } else {
#               // 新規コメント
#               await github.rest.issues.createComment({
#                 issue_number: context.issue.number,
#                 owner: context.repo.owner,
#                 repo: context.repo.repo,
#                 body: report
#               });
#             }
# 
#       - name: Fail if issues found
#         if: steps.report.outputs.has_issues == 'true'
#         run: |
#           echo "::warning::ドキュメント整合性の問題が見つかりました。詳細はPRコメントを確認してください。"
#           # 警告のみ。失敗させたい場合は exit 1
