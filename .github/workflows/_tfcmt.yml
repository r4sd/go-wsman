# ============================================
# このワークフローは無効化されています
# 有効化するには /setup-cicd スキルを使用してください
# ============================================
#
# name: tfcmt Plan Comment
# 
# on:
#   workflow_call:
#     inputs:
#       pr-number:
#         description: 'Pull request number'
#         required: true
#         type: number
#       terraform-version:
#         description: 'Terraform version'
#         required: true
#         type: string
# 
# env:
#   TERRAFORM_DIR: clouds/aws/dev
# 
# jobs:
#   tfcmt:
#     name: tfcmt Plan Comment
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       pull-requests: write
#       id-token: write
# 
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
# 
#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ inputs.terraform-version }}
# 
#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::852586641538:role/GitHubActionsTerraformRole
#           aws-region: ap-northeast-1
# 
#       - name: Install tfcmt
#         run: |
#           curl -fsSL https://github.com/suzuki-shunsuke/tfcmt/releases/latest/download/tfcmt_linux_amd64.tar.gz | tar -xz -C /tmp
#           sudo mv /tmp/tfcmt /usr/local/bin/
#           tfcmt --version
# 
#       - name: Terraform Init
#         run: |
#           cd ${{ env.TERRAFORM_DIR }}
#           terraform init
# 
#       - name: Run tfcmt plan
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           cd ${{ env.TERRAFORM_DIR }}
#           tfcmt --config ../../../.github/tfcmt.yml plan --patch -- terraform plan -no-color
